# 差分アルゴリズムは閾値付きピクセル比較から始め、拡張可能な設計にする

PDF差分のオーバーレイ生成アルゴリズムとして、まず閾値付きピクセル比較を実装する。アンチエイリアスや微小なレンダリング差異を許容するため、ピクセルごとのRGB差分が閾値を超えた場合のみ「変更あり」と判定する。

アルゴリズム部分はトレイトとして抽象化し、将来的に位置合わせ（レジストレーション）や知覚的ハッシュなど精度の高い手法に差し替え可能にする。

## Consequences

初期実装はシンプルだが、閾値の適切な値はA3建築図面の実データで調整が必要。閾値はユーザー設定として公開することも検討する。
