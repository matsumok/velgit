# フロントエンドの非同期状態管理にTanStack Queryを使用する

ReactからのTauriコマンド呼び出し（`invoke()`）の非同期状態管理にTanStack Queryを採用する。`invoke()` をクエリキーと非同期関数として扱うことで、ローディング状態・エラー処理・キャッシュ・再取得を宣言的に管理できる。

Tauriのリアルタイムイベント（ファイル変更通知など）の受信はZustandと組み合わせ、イベント受信時にTanStack Queryのキャッシュを無効化してUIを更新する。
