# Backend Conventions

Conventions for Rust / Tauri backend code in this project.

## Formatting

Run the appropriate formatter before every commit.

| Target | Tool | Command |
|--------|------|---------|
| `.rs` | rust-analyzer (`rustfmt`) | save in editor / `cargo fmt` |

Do not commit unformatted files.
